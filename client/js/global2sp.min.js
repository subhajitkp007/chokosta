window.addEventListener('resize', resizecanvas);
documentelemId["GameCanvas"].addEventListener("load", createcanvas());
window.addEventListener("resize", resizecanvas());
documentelemId["canvaspart"].addEventListener("load", eventlisten());
let btn = documentelemId["helpbutton"];
btn.addEventListener("click", function() {
    let bgo = document.querySelector(".modal-bg");
    bgo.style.opacity = 1;
    bgo.style.visibility = "visible";
    bgo.style.display = "inline";
});
let btnclose = documentelemId["closebutton"];
btnclose.addEventListener("click", function() {
    let bgo = document.querySelector(".modal-bg");
    bgo.style.opacity = 0;
    bgo.style.visibility = "hidden";
    resizecanvas();
});
drawanimedicestart();
