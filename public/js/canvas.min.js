function createcanvas(){canvas=documentelemId["GameCanvas"]; if (canvas.getContext){W=$(window).width(); H=$(window).height(); TW=W; H=Math.min(W, H); W=H; canvas.width=TW; canvas.height=H; ctx=canvas.getContext('2d');}updatewindow();}function resizecanvas(){createcanvas(); updatewindow();}